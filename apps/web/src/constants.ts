// Workspace-wide brand constants for the web app.
//
// The user-facing product name lives in one place so the home view,
// entry shell, packaging metadata, and any future surface read from
// the same source instead of hard-coding "Open Design" string literals.
// Keep this list narrow — only add values that are user-visible and
// shared across multiple surfaces.

export const APP_NAME = 'Curriculum Workspace';
